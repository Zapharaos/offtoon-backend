# Offtoon Backend Environment Configuration
# Copy this file to .env and update with your settings

# Environment (leave empty for dev, set to "prod" for production)
APP_ENV=

# Backend port
BACKEND_PORT=3000

# Docker Compose file selection
# Development (default): docker-compose.dev.yml
# Production: docker-compose.prod.yml
COMPOSE_FILE=docker-compose.dev.yml

# Add any other configuration overrides here using OFFTOON_ prefix
# Example:
# OFFTOON_LOGGER_LEVEL=debug
